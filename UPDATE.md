変更点まとめっす。
ビルド環境の刷新から新機能の追加、UIの改善までかなり手広く手を入れたっす。

### 🚀 新機能・機能強化

*   **NG機能の大幅拡張**
    *   **ハイライト機能**: NG条件にヒットしたスレを「非表示」にするのではなく、「色付き・ラベル付き」で強調表示可能に（例: `HighlightTitle(label=VTuber, bgColor=pink):...`）。
    *   **スコープ指定**: NGワードの適用範囲をドメインや板単位で指定可能に（例: `(bbs.eddibb.cc):荒らし`）。
    *   **スレ一覧での反映**: ハイライトされたスレは一覧の上部に優先表示され、指定した背景色が適用されます。
*   **スレッド内フィルタリング機能**
    *   ヘッダーに「フィルタボタン」を追加。
    *   「人気レス（返信3以上）」「画像・動画のみ」「外部リンクのみ」などでレスを即座に絞り込み表示可能になりました。
*   **「ライブスタイル」モード**
    *   自動更新時に、新着レスを一気に表示するのではなく、書き込み時間差（または倍速設定）に合わせてチャットのようにパラパラと順次表示するモードを追加。
*   **メディアズームの改善**
    *   画像/動画の拡大方法を「ホバー」か「クリック（トグル）」か選択可能に。
    *   拡大率をプリセット選択式に変更。
*   **マウスジェスチャー機能**
    *   スレッド画面で右クリックしながらドラッグすることで、ページトップ/ボトムへのスクロールが可能に。
    *   上へドラッグ: ページトップへ移動
    *   下へドラッグ: 最新レス（最下部）へ移動
*   **リプライツリー表示**
    *   リプライリンクを右クリックすることで、そのレスに対する返信ツリーをポップアップで階層表示する機能を追加。
*   **スレッドのポップアウト表示**
    *   スレッドを独立したウィンドウで開く機能を追加（ヘッダーのツールメニュー内）。
    *   独立ウィンドウでもブックマーク同期や自動リロードが正常に動作するように内部ロジックを改修。

### 🛠 UI/UX 改善

*   **スレッド一覧のカスタムツールチップ**: ブラウザ標準のツールチップをやめ、即座に表示されるカスタムツールチップを実装（NG理由や長いタイトル確認用）。
*   **ミニマップ（スレッド内）**
    *   右端に固定表示されるミニマップを追加。
    *   レスポンシブ設計: ビューポート幅の 8%（最小 30px、最大 200px）で幅を算出。ウィンドウリサイズやコンテンツ変更で幅を同期して表示を更新します。
*   **コピー機能の拡充**:
    *   「スレッド全文をテキストコピー」
    *   「NGされたレスのみを抽出してコピー」
    *   レス右クリックメニューに「レス単体をコピー」を追加。
*   **ヘッダー/フッター**:
    *   スレッドヘッダーに「ブックマーク追加/解除」ボタンを配置。
    *   フッターの「次の未読へ」ボタンの表示/非表示設定を追加。
    *   スレッド情報の表示（レス数・勢い）を調整。

### 🌐 対応サイト・コアロジック

*   **新規サイト対応**: `bbs.eddibb.cc`（エッヂ）への対応を追加。
*   **URL解析のリファクタリング**: `src/core/URL.ts` をTypeScriptで全面的に書き直し、各BBS形式（5ch, したらば, まちBBS, eddibb）の判定ロジックを整理。
*   **Config機構**: iframe内外で設定にアクセスしやすくするため、Config呼び出しをProxy化。

### ⚙️ 開発環境・ビルドシステム

*   **Rolldownへの移行**: バンドラーを `Rollup` からRust製の高速バンドラー `Rolldown` に変更。ビルド速度の向上。
*   **E2Eテストの導入**: `Playwright` を導入し、拡張機能を読み込んだ状態での自動テスト環境（`tests/`）を構築。
*   **ドキュメント**: AIエージェント向けのプロジェクト概要説明ファイル `AGENTS.md` を追加。
