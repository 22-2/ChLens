# ChLens MCP

ChLensのChrome版に保存されているスレッドログを、MCP対応エージェントから読み取るための接続機能です。取得・解析・保存はChromeのサービスワーカーが担当するため、ChLensの画面を開いていない状態でも利用できます。オフスクリーンドキュメントや追加の常駐依存は使わず、拡張機能標準の`fetch`・IndexedDB・WebSocketだけで構成しています。

## できること

- URLを指定したスレッドの取得
- URLを省略した現在表示中スレッドの取得
- 保存済みログの検索
- 未保存スレッドの新規取得と保存
- 先頭・末尾・レス番号範囲・人気レスによる絞り込み
- 直近の書き込み履歴の取得（投稿先URL・レス番号・本文付き）
- 直近の閲覧履歴の取得（重複排除・新しい順）
- レスの返信先、被返信先、返信数、アンカー数を含むTOON出力
- 指定レス・参加者IDを中心にした議論判定用の返信ツリー作成
- 判定結果のテキスト・JSON・Markdown・HTML・PNG保存

`read_thread` の `mode` は次の3種類です。

- `auto`: キャッシュを使い、必要なら通常の取得処理を行う（既定）
- `cache`: 保存済みログだけを使う。保存がなければエラーにする
- `refresh`: 新着を取得して保存してから返す

絞り込みを指定しない場合も、1回の応答は最大500レスに制限します。続きが必要な場合は `start` / `end` または `last` を指定してください。

## 履歴の取得

`get_write_history` は直近の書き込み履歴を、`get_browsing_history` は直近の閲覧履歴をTOONで返します。どちらも `query`（省略時は直近順）と `limit`（1〜100、既定20）を受け付けます。

- `get_write_history`: 投稿先URL・レス番号・スレタイ・名前・メール・本文・投稿日時を含みます
- `get_browsing_history`: 同じスレの重複を除いて新しい順に並べ、URL・スレタイ・板名・閲覧日時を含みます

日毎に見る場合は、どちらのツールにも `date` を `YYYY-MM-DD` 形式で渡します。指定日（ブラウザの現地時間）の履歴だけを返し、不正な日付はエラーになります。

## 起動

リポジトリのルートで次を実行します。

```text
pnpm mcp:server
```

MCPクライアントの設定例です。実際の設定ファイルの形式やキー名はクライアントに合わせてください。

```json
{
  "mcpServers": {
    "chlens": {
      "command": "pnpm",
      "args": ["mcp:server"],
      "cwd": "V:\\repos\\fork\\read.crx-2"
    }
  }
}
```

サーバーは標準入出力でMCPを受け付け、MCPの初期化時に `127.0.0.1:52539` の中継へ接続します。最初のプロセスだけが排他的に待ち受けを開始し、別チャットのプロセスは既存の中継を共有します。サービスワーカーはWebSocketで接続し、未接続時はアラームで再接続を試みます。

各チャットの利用状態は持続するHTTP接続で管理します。標準入力の切断・終了シグナルではそのチャットの接続を解放し、最後の利用者が切断して約1秒後に中継も終了します。中継を起動したチャットが終了しても、他の利用者がいる間はそのプロセスが中継を維持します。チャットごとにNodeプロセスがあっても、Chrome用の待ち受けは1つです。常駐プログラムの登録は必要ありません。

中継プロセスが強制終了した場合、残ったチャットは次のツール呼び出しで中継の再起動・再接続を試みます。途中で切れた操作は自動再送しません。Chromeの再接続まではツールが未接続エラーを返すことがあります。

共有に未対応の旧サーバーが起動している場合は、旧サーバーを終了してCodexを再起動してください。Chrome側の接続先は変わらないため、この変更だけなら拡張機能の再ビルドは不要です。

## 安全上の扱い

- 中継はループバックアドレスだけで待ち受けます。
- MCPサーバーが動いていない間、サービスワーカーはWebSocket接続を閉じたまま、アラームのタイミングで再接続します。
- レス本文はユーザーが要求した範囲だけを返します。
- 現在の実装では、MCPサーバーの標準エラーに診断情報を出し、標準出力にはMCPのJSON-RPCだけを書きます。

## 現在の制約

Chromeを終了している場合はサービスワーカーも動作しません。Chromeが起動していれば、ChLensの画面を閉じていてもログの読み書きができます。サービスワーカーは常駐を保証されないため、未接続時はアラームによる再接続待ちになります。

MCPのTOONは画面側のDOM依存のNG表示判定を通さず、取得したレスを返します。AIから元ログを検索・分析する用途を優先した設計です。

Chrome版の拡張機能IDやNative Messagingの登録は使わないため、開発ビルドと通常インストールを切り替えるたびに登録し直す必要はありません。

## 議論判定

`prepare_debate` は、`url` と `responseNumbers`（レス番号）または `participantIds`（ID）を受け取り、指定対象から返信元・返信先を辿ったTOONを返します。`contextDepth` で辿る深さ、`maxResponses` で返す上限を指定できます。対象レスは `role: target`、理解のために補ったレスは `role: context` として区別されます。

返されたTOONの `instructions` と `resultSchema` に従って、呼び出し元のAIが判定JSONを作成します。判定結果は固定の二陣営ではなく、争点ごとの `issues` と参加者ごとの `participants` で表現します。罵倒や煽りを根拠として扱わず、判断材料が足りない場合は `insufficient-evidence` と不確実性を記録してください。

`save_debate_result` に判定JSONを渡すと、既定で次の形式を同じベース名で保存します。

- `json`: 再利用可能な正本データ
- `markdown`: レス番号へのリンク付きレポート
- `text`: プレーンテキスト
- `html`: 全文を折り返して表示する判定カード
- `png`: HTMLをChromiumで画像化した判定カード

保存先は、`CHLENS_DEBATE_OUTPUT_DIR` を指定した場合はそのディレクトリ、未指定時はユーザーのホームディレクトリにある `ChLens/debate-results` です。`formats` で必要な形式だけに絞れます。画像は内容の高さに合わせて伸び、争点・参加者・根拠を省略しません。HTML形式はPNGのレイアウト確認や再利用に使えます。PNG出力にはPlaywright用Chromiumが必要です。未導入の場合は `pnpm exec playwright install chromium` で追加します。
