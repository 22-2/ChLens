extends ../_base.pug

block vars
  - var name = "browser";
  - var title = "read.crx-2";
  - var htmlClasses = ["view", "view_browser"];
  - var needCore = true;
  - var needCss = true;

block body
  #root
